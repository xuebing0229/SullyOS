{
  "name": "sullyos-instant-push-worker",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "SullyOS Instant Push — Cloudflare Worker (deploy-from-git entry).",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@rei-standard/amsg-instant": "^0.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
