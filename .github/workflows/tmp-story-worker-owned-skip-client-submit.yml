name: Temporary worker-owned submit guard

on:
  push:
    paths:
      - '.github/workflows/tmp-story-worker-owned-skip-client-submit.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - name: Apply patch
        run: python .github/scripts/patch_story_worker_owned_skip_client_submit.py
      - name: Verify diff
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add utils/backgroundImageJobs.ts
          git commit -m "fix: never client-submit worker-owned story images"
          git push origin master
